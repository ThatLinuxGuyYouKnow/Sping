import 'package:flutter/material.dart';

class ConverterScreen extends StatelessWidget {
  ConverterScreen({super.key});

  Widget build(BuildContext) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Column(
          children: [Text('Png To Svg Converter')],
        ),
      ),
    );
  }
}
